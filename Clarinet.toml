[project]
name = "PredictStack"
description = "A peer-to-peer prediction market platform on Stacks"
authors = []
telemetry = true
cache_dir = "./.cache"

# SIP-010 Trait Definition
[contracts.sip010-trait-v1]
path = "contracts/sip010-trait-v1.clar"

# USDCx Mock Token
[contracts.usdcx-v1]
path = "contracts/usdcx-v1.clar"

# Main Prediction Market Contract
[contracts.prediction-market-v1]
path = "contracts/prediction-market-v1.clar"

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }
