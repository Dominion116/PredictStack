[project]
name = "PredictStack"
description = "A peer-to-peer prediction market platform on Stacks"
authors = []
telemetry = true
cache_dir = "./.cache"

# SIP-010 Trait Definition
[contracts.sip010-trait-v1]
path = "contracts/sip010-trait.clar"

# USDCx Mock Token
[contracts.usdcx-v1]
path = "contracts/usdcx.clar"

# Main Prediction Market Contract V6 (Latest)
[contracts.prediction-market-v6]
path = "contracts/prediction-market-v6.clar"

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }
