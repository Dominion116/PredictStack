# ─── Chainhook Observer ───────────────────────────────────────────────────────

# "testnet" or "mainnet"
NEXT_PUBLIC_NETWORK=testnet

# The port your local observer server listens on (avoid 3000, that's Next.js)
PORT=3001

# A random secret — Chainhook will include this in every webhook Authorization header
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CHAINHOOK_AUTH_TOKEN=replace_with_a_random_secret_min_16_chars

# For local development: use ngrok to expose port 3001
#   npx ngrok http 3001
# Then paste the https URL below (update it each time ngrok restarts)
CHAINHOOK_PUBLIC_URL=https://xxxx-xxxx-xxxx.ngrok.io

# Hiro-hosted Chainhook node (no self-hosting needed with v2)
# Testnet:
CHAINHOOK_NODE_URL=https://api.testnet.hiro.so/chainhooks/v2
# Mainnet (comment out testnet line above and use this instead):
# CHAINHOOK_NODE_URL=https://api.hiro.so/chainhooks/v2
